package com.example.jamkkaebi.artifact.domain;

/**
 * 시대. 콘텐츠 순서대로 선언한다 — 도감·친구 카드의 시대별 복원 수가 이 순서로 나열된다.
 */
public enum Era {
    THREE_KINGDOMS, // 삼국 ~ 통일신라
    GORYEO,         // 고려
    JOSEON          // 조선
}
